---
import Base from '../layouts/Base.astro';
import Dashboard from '../components/Dashboard.svelte';
import { loadClusters } from '../lib/scrambler/load-content';

const clusters = await loadClusters();
---

<Base>
  <Dashboard
    client:only="svelte"
    initialClusters={clusters}
  />
</Base>
